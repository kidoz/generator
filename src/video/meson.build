# Video Subsystem
# Shared scanline rendering: the paletted-to-native conversion every backend
# uses, the CRAM snapshot the core publishes into it, and the DIB helper.
# The VDP itself lives in the machine (src/vdp).

video_sources = files(
  'uiplot.cpp',
  'uiplot_cram.cpp',
  'dib.cpp'
)
